spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medtrustgraph
    username: root
    password: 292902
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080